import { colors } from "./index";

const dark = {
	colors: {
		accent: colors.purple,
		cardBackground: colors.gray5,
		background: colors.gray6,
		text: colors.white,
		subText: colors.gray2,
		cardShadow: "0 2px 4px rgba(0, 0, 0, 0.38)",
		cardShadowHover: "0 4px 8px rgba(0, 0, 0, 0.38)"
	}
}

export default dark;